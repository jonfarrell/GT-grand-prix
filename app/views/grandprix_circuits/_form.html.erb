<%= form_with(model: grandprix_circuit) do |form| %>
  <% if grandprix_circuit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(grandprix_circuit.errors.count, "error") %> prohibited this grandprix_circuit from being saved:</h2>

      <ul>
        <% grandprix_circuit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
