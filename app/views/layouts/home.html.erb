<!DOCTYPE html>
<html>
  <head>
    <title>GT Grand Prix</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0,user-scalable=0"/>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>


  </head>

  <body>
    <nav class="home-nav nav-collapse">
      <div class="nav-logo"></div>
      <ul>
        <% if user_signed_in? %>
          <li><%= link_to('Lists', lists_path) %></li>
          <li><%= link_to "Settings", edit_user_registration_path %></li>
          <li><%= link_to "Sign out", destroy_user_session_path, method: :delete %></li>

        <% else %>
          <li><%= link_to 'Home', page_path('home') %></li>
          <li class="home-login"><%= link_to 'Log in', new_user_session_path %></li>
          <li class="home-sign-up"><%= link_to 'Sign Up', new_user_registration_path %></li>
        <% end %>
      </ul>
    </nav>

    <div class="home">
      <%= yield %>
    </div>

    <script>
      var nav = responsiveNav(".nav-collapse");
    </script>

  </body>
</html>
